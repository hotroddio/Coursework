// Complete the factorial function so that it returns the factorial for the given positive integer using recursion. 

n = 5

// 5! = 5 *4 * 3 * 2 * 1

const factorial = (n) => {
    if(n === 0) {
        return 1;
    } else {
        return n * factorial(n-1);
    }
}


if (arrA[0] > arrB[0]) { 
    push 
}